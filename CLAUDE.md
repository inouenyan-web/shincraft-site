# CLAUDE.md（リポジトリ全体）

## 🔴 全セッション共通ルール（最優先・恒久）

**軸：すべての作業で、井上さんが手作業でコピペや値の橋渡しをしなくて済む構造を最優先で設計する。**

### 🔴 学習共有の原則（2026-06-08 追加・足並みを揃える）

- **`LESSONS.md`（リポジトリ直下）は全セッション必読**。井上さんからの指摘・確立ルール・
  技術教訓・接続済みコネクタの棚卸しが記録されている。
- **新しい指摘を受けたセッションは、その場で `LESSONS.md` に追記してpushする**。
- 毎朝6時に `daily-briefing.yml` が LESSONS.md＋運用ボードをDriveの
  「AI共有ブリーフィング_最新.md」（AI親フォルダ）へ自動転送する。
  Chrome版Claude・コワーク・ChatGPT への指示プロンプトには
  **「まずDriveのAI共有ブリーフィングを読め」を含める**こと。
- **「できない」と言う前に、接続済みコネクタ（MCP・プラグイン）を全部棚卸しして実測検証する。**
  （教訓：Square MCPが接続済みなのに見落とし、井上さんに指摘された）

### 🔴 全員検討の原則（2026-06-08 追加・ピラミッド式委譲の禁止）

判断が必要なタスク（手段選び・委譲・井上さんへの作業依頼を伴うもの）では、
**1セッションの単独判断で直列に委譲してはならない**（自分→Chrome版→井上さん、と
順に投げ下ろすピラミッド式の禁止）。代わりに：

1. **毎回、最初に Agent ツールで検討チームを並行起動して全員会議を開く**
   （調査担当・代替設計担当など複数視点。例外なく毎タスク実施）。
2. 「誰が・どこを・どれだけ介入するのが最適か」を全員の結果を統合して決めてから動く。
3. 教訓例：Metaトークン問題は単独判断では「Secrets3つの手動登録」で停滞したが、
   並行検討で「無期限システムユーザートークンにより作業自体を消す」という上位解が出た。
   単独判断は局所最適に陥る。**作業を減らす最良の方法は、作業を不要にする設計を全員で探すこと。**

4. **（2026-06-16 強化）井上さんが Claude Code に出す指示は、例外なく全員会議を起点にする。**
   井上さんは「軽い指示」を出さない。したがってセッション側が「これは軽いから直接やろう」と
   勝手に判断して全員会議を省略してはならない。指示を受けたら、即答せず、まず Agent ツールで
   検討チームを並行起動して統合判断してから動く。省略してよいのは**純粋な事実確認1問への即答のみ**
   （例：「LINEは無料？」）。判断・設計・委譲・実装を少しでも伴うなら必ず全員会議を開く。

### 🔴 遊休ゼロの原則（2026-06-08 追加・ヒマを持て余すな）

- **担当タスクが人間待ち・外部待ちでブロックされたら、待っている間に余った稼働を
  運用ボードの他の優先タスクの補助に自動的に回す**（指示を待たない）。
- 例：トークン設定が井上さんの操作待ちの間に、次の優先タスク（商品一元管理のAPI調査、
  撤去計画の作成など）をAgentで並行起動して進めておく。
- セッション終了時、進めた補助作業も運用ボードに記録して次セッションへ引き継ぐ。

- 設定済みのAPIキー・Webhook・GAS連携などは、**人間を介さず直接呼び出す**。
- **コピペが必要な手順を提示する前に、まず自動化や直接連携で代替できないかを必ず検討し、
  代替できる場合はそちらを採用する。**
- これは秘書セッションに限らず、**各セッションが単独で動作している場合も常に適用する共通の前提**とする。
- 井上さんへの確認は、SNS投稿前の承認のような意図的な人間ゲートに限る。値の受け渡しのための
  手作業は原則ゼロを目指す。

### 🚫 井上さんに作業させてよい範囲（厳守・2026-06 の反省を反映）

井上さんに手を動かしてもらうのは、**技術的・権限的に本人にしかできない操作だけ**に限定する。
具体的には次のみ許容：
1. Google等の **OAuth/認証・アクセス許可ダイアログの承認**
2. **公開デプロイの最終確定ボタン**（内容確認のため本人が押す）
3. **Claude Code on the web の環境変数・環境設定の変更**（セッションからは変更不可）
4. SNS投稿前など、**意図的な人間ゲートとしての承認**

上記以外（値のコピペ、ファイルの受け渡し、台帳更新、アップロード等）は**すべてClaude Code側で自動実行する**。

**やってはいけないこと：**
- 同じコピペ手順を**何度も投げ直す**こと。1回で通らなければ即、別アプローチに切り替える。
- 委譲先（Chrome版等）に、**その委譲先が実際には実行できない作業**を振ること。
  振る前に「この相手はこの操作を物理的にできるか？」を必ず確認する。
- 作業を頼む前に必ず自問する：**「これは本当に井上さん本人にしかできないか？ 自動化できないか？」**
  少しでも自動化余地があれば、頼む前に自分で試す。
- **生の値（トークン・URL等）を井上さんに渡して「あなたが入力して」と丸投げすること。**
  値の入力・画面遷移は Chrome版／自動化に **「確定（保存）の直前まで」必ずやらせる**。
  井上さんに残してよいのは **最後の確定操作1つだけ**。
  （例：Claude Code環境変数の登録 → Chrome版が設定画面を開き名前と値を入力 →
   井上さんは保存ボタンを押すだけ。「不可だから本人がやれ」と全部投げない。
   本人に必要なのは"最終確定"であって"入力作業"ではない、を常に区別する。）
- **Chrome版プロンプトを作る前に必ず確認する：「井上さんが直接やる方が速くないか？」**
  Chrome版経由は「プロンプト作成 → 貼り付け → Chrome版の実行 → 報告」という4手順になる。
  「URLを開いて値を貼るだけ」なら井上さんに直接やってもらう方が圧倒的に速い。
  Chrome版が本当に必要なのは、ログインが要る画面操作・長い自動スクレイピング等に限定する。

---

**Claude Code が井上さんの代わりに運用を統括します。** まず Claude Code 自身で処理し、
できない時のみ次の優先順位で委譲してください：**コワーク → Codex → Yoom → Chrome版Claude**。

主な作業は `ai-sns-automation/` に集約しています。運用の中核ルール・パイプライン・
固定ID・委譲ポリシーは **`ai-sns-automation/CLAUDE.md`** を参照してください。

スマホからの定型操作：
- `/sns` … SNS投稿パイプラインを進める
- `/note-x` … noteの新着記事をXへ投稿する

秘密情報（APIキー等）は Git に保存せず、環境変数で渡します
（`ai-sns-automation/SETUP_SECRETS.md`）。


## 📥 受信箱4回チェック（0/6/12/18時）の共通認識（2026-06-30 追加）

ShinCRAFTの受注取りこぼし防止のため、1日4回（JST 0/6/12/18時）の受信箱チェックが恒久稼働中。**コワークもクロードコードも、毎セッション開始時に最新結果を必ず読んで認識を揃えること。**

- **最新の受注・要対応は `運用ボード.md` を読む**（全チャネル横断。`- [ ]` が未対応）。
  - `📧 Gmail` / `💬 Chatwork`（既読精査）/ `📩 Instagram投稿` … GitHub Actions `daily-briefing.yml` が0/6/12/18時に自動更新（コード/Actions側の担当）。
  - `IG_DM_MANUAL` セクション … Instagramのお客様DMはAPI不可のため、コワークが Meta Business Suite を目視して受注・要対応を抽出・反映（App Review承認後はActions側 `check_instagram_dm.mjs` に自動化が切替）。
- 恒久ルールは `受信箱チェック_運用ルール.md`、当日サマリは `AI共有ブリーフィング_最新.md` を参照。
- **担当分担**：自動取得可（Gmail/Chatwork/IG投稿）＝Actions（コード側）、API不可のIG DM＝コワークが目視。結果は同じ `運用ボード.md` に集約し、コワーク/コード/ChatGPTで共通認識する。
- 🔴最優先＝「相手の最後の発言が未返信＝対応待ち」。先頭に強調する。
