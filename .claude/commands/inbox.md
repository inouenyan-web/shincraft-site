---
description: 受注取りこぼし防止チェック。運用ボードから全チャネルの未対応・要対応を集約して報告する
---

ShinCRAFTの受注取りこぼし防止チェック。以下を実行し、日本語で簡潔に報告すること。

1. リポジトリ直下の `運用ボード.md` を読む（0/6/12/18時に GitHub Actions＋コワークが更新する最新の受信箱結果）。
2. 次のセクションから未対応タスク（`- [ ]`）を抽出・集約する：
   - `📧 Gmail 新着チェック`（受注関連メール）
   - `💬 Chatwork 新着チェック`（既読精査の受注・途中）
   - `📩 Instagram` 投稿系
   - `IG_DM_MANUAL`（コワークが Meta Business Suite を目視して反映した Instagram DM の受注・要対応）
3. 並び順：🔴最優先＝「相手の最後の発言が未返信＝対応待ち」を先頭に、次に🟡受注関連・フォロー。
4. 恒久ルールは `受信箱チェック_運用ルール.md`、当日サマリは `AI共有ブリーフィング_最新.md` も必要に応じ参照。
5. 報告は「①🔴要対応（未返信） ②🟡受注関連・フォロー ③気づき/注意」の3部構成で簡潔に。

※このコマンドは「読んで報告」まで。返信やデータの書き換えは行わない（必要なら井上さんに確認する）。
